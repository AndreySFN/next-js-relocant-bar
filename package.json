{
  "name": "nest-js-app-router",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "set BASE_URL=localhost:4000 && next dev",
    "build": "rm -rf ./.next && next build",
    "build:prod": "rm -rf ./.next && NODE_ENV=production next build",
    "build:start": "rm -rf ./.next && next build && next start",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "bun": "^1.1.3",
    "dotenv": "^16.4.5",
    "next": "14.1.4",
    "object-hash": "^3.0.0",
    "react": "^18",
    "react-dom": "^18",
    "styled-components": "^6.1.8"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "eslint": "^8",
    "eslint-config-next": "14.1.4",
    "typescript": "^5"
  }
}
